from django.urls import path
from . import views

urlpatterns = [
    # path('schedules/<int:schedule_id>/', views.as_view(), name='schedule'),
    # TODO: path 설정 /schedules?workspace_id={workspace_id}&&date={date}'
]
